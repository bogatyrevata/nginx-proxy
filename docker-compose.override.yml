# Локальная разработка — порты открыты для прямого доступа
# Docker Compose применяет этот файл автоматически поверх docker-compose.yml
services:
  db:
    ports:
      - "5434:5432"

  api:
    ports:
      - "8000:8000"

  frontend:
    ports:
      - "3000:80"
